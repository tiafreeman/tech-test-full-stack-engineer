import React from "react";

import DisplayConsoleContainer from "./styles";

const DisplayConsole = (capsules, buttonPressed) => {
  console.log(capsules);
  return (
    <DisplayConsoleContainer>
      <p>hello</p>
    </DisplayConsoleContainer>
  );
};

export default DisplayConsole;
